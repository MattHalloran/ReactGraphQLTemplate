import styled from 'styled-components';
export const StyledNavbar = styled.nav`
    .right-align {
        position: relative;
        padding-right: 20px;
    }

    .nav-list {
        display: flex;
        list-style: none;
        margin-top: 0px;
        margin-bottom: 0px;
    }

    .nav-item {
        position: relative;
        padding: 5px;
    }

    .nav-link {
        color: white;
        text-decoration: none;
    }

    .nav-logo {
        max-height: 50px;
        margin-right: 5px;
        margin-bottom: 15px;
    }
`;