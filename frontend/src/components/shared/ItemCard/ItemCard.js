import React, { memo } from 'react'
import { StyledItemCard } from './ItemCard.styled';

function ItemCard() {
    return (
        <StyledItemCard>

        </StyledItemCard >
    );
}

ItemCard.propTypes = {

}

export default memo(ItemCard);