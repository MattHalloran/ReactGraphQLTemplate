import styled from 'styled-components';

export const StyledSlider = styled.div`
    position: relative;
    height: 100vh;
    width: 100vw;
    margin: 0 auto;
    overflow: hidden;
    white-space: nowrap;
`;