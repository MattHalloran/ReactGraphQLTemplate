import { GraphQLEnumType } from 'graphql';
import { ORDER_STATUS } from '@local/shared';

export const TraitNameType = GraphQLEnumType({
    name: 'Trait name',
    description: 'A plant attribute type',
    values: {
        
    }
})